import { colors } from './Colors'

export default () => {
  return (
    {
      map: {
        top: '73px',
        left: 0,
        right: 0,
        width: '100%',
        bottom: 0,
        position: 'absolute'
        }
    }
  )}