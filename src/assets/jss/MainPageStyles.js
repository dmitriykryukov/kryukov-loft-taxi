import { colors } from './Colors'

export default () => {
  return (
    {
      wrapper: {
        overflow: 'hidden',
        minHeight: '100%',
        position: 'relative'
      },
      main: {
        height: 'calc(100vh - 73px)',
        position: 'relative',
      }
      
    }
  )}